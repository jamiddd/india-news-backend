fastapi>=0.110.0
uvicorn[standard]>=0.28.0
sqlalchemy[asyncio]>=2.0.28
asyncpg>=0.29.0
alembic>=1.13.1
pydantic>=2.6.4
pydantic-settings>=2.2.1
httpx>=0.27.0
feedparser>=6.0.11
trafilatura>=1.8.0
curl_cffi>=0.13.0  # impersonates real browser TLS fingerprints — several sources
                   # (Gadgets360, Indian Express) 403 plain httpx/curl at the WAF
                   # (Akamai etc.) regardless of headers; this gets past that.
google-auth>=2.29.0
requests>=2.31.0  # required by google.auth.transport.requests
firebase-admin>=6.5.0  # verifies Firebase ID tokens for email/password + Google Sign-In
redis>=5.0.3
slowapi>=0.1.9  # per-IP rate limiting; backed by the redis instance above so
                # limits are shared correctly across the app's 4 uvicorn workers
python-dotenv>=1.0.1
simhash>=2.1.2
